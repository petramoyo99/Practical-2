# Practical 2 — SQL JOIN Practice

## Overview
This practical focuses on mastering SQL JOINs using these datasets:
orders_large.csv, customers_large.csv, products_large.csv

## Task
Import the datasets into a SQL database and answer the questions using:
INNER JOIN, LEFT JOIN, RIGHT JOIN, FULL OUTER JOIN

## Requirements
- One query per question
- Return exactly the required columns
- Use aliases and clean formatting
- Use UNION for FULL OUTER JOIN if DB doesn't support it

## Submission
Submit a `.sql` file named:
`Practical_2_SQL_Joins.sql`
